// @flow

// StorageArea.remove(string or array of string keys, function callback)

// migrate old todo path to current

// migrate settings too

// todo address

// 'history' ==> totalTabsSnoozedCount ?? or just implement history and thats it

// tabs array instead of keys

// .tsVersion
// tabCount
// keys => arraw

// sync => local
// also move tabs from keys to array
// tab.period.time => hour

// rewrite settings too. (remove uncessery keys)
// Write everything to local.oldData just in case

// snooze.snoozeOptionIndex (number) => type (string)
